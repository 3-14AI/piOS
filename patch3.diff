diff --git a/kernel/src/wasm/linker.rs b/kernel/src/wasm/linker.rs
index a276b91..1c94b7f 100644
--- a/kernel/src/wasm/linker.rs
+++ b/kernel/src/wasm/linker.rs
@@ -370,30 +370,14 @@ mod tests {
         linker.link(main_wasm).unwrap();
     }

-    #[test]
-    fn test_cranelift_compiler_backend_integration() {
-        let mut linker = WasmComponentLinker::new();
-
-        // The `test_wasi.wasm` includes the cranelift integration
-        // This tests that we can load the compiled module into the linker
-        // and that it resolves its WASI dependencies.
-        let wasm_bytes = include_bytes!("../../../test_wasi/target/wasm32-wasip1/debug/test_wasi.wasm");
-
-        let result = linker.link(wasm_bytes.to_vec());
-        // Depending on whether we want to run it, linking itself shouldn't fail due to imports
-        assert!(result.is_ok());
-    }
-
     #[test]
     fn test_cranelift_compiler_backend_integration() {
         let mut linker = WasmComponentLinker::new();

         // The `test_wasi.wasm` includes the cranelift integration
-        // This tests that we can load the compiled module into the linker
-        // and that it resolves its WASI dependencies.
         let wasm_bytes = include_bytes!("../../../test_wasi/target/wasm32-wasip1/debug/test_wasi.wasm");

-        let result = linker.link_component(wasm_bytes.to_vec());
+        let result = linker.link(wasm_bytes.to_vec());
         // Depending on whether we want to run it, linking itself shouldn't fail due to imports
         assert!(result.is_ok());
     }
