[package]
name = "kernel-fuzzer"
version = "0.1.0"
edition = "2021"

[dependencies]
arbitrary = { version = "1.3", features = ["derive"] }
libfuzzer-sys = "0.4.7"
kernel = { path = "../../kernel" }
inference_runtime = { path = "../../libs/inference_runtime" }

[workspace]
