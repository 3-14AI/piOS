[package]
name = "power_governor"
version = "0.1.0"
edition = "2021"

[lib]
name = "power_governor"
path = "src/lib.rs"

[[bin]]
name = "power_governor"
path = "src/main.rs"
test = false

[dependencies]
inference_runtime = { path = "../../../libs/inference_runtime" }

[lints]
workspace = true
