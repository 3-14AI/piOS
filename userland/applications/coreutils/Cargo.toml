[package]
name = "coreutils"
version = "0.1.0"
edition = "2021"

[dependencies]
pios-api = { path = "../../../libs/pios-api" }

[lib]
name = "coreutils"
path = "src/lib.rs"
crate-type = ["cdylib", "rlib"]
