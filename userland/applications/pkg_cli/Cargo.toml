[package]
name = "pkg_cli"
version = "0.1.0"
edition = "2021"

[dependencies]
pios-api = { path = "../../../libs/pios-api" }
package_manager = { path = "../../services/package_manager" }

[lib]
name = "pkg_cli"
path = "src/lib.rs"
