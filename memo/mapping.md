# Mapping
- **WP-001**: `kernel/src/boot.rs`, `kernel/src/verifier.rs`, `kernel/src/main.rs`, `tools/install_verus.sh`, `tools/build_kernel.sh`, `tools/verify_kernel.sh` - Setup Verus toolchain and UEFI boot stub.

## Themes
- **Verus Toolchain**: Setup and configuration of the verification environment.
- **UEFI Boot**: Initial system startup and transition to verified kernel.
